import type { TablaReporteResultado } from '../tabla-reporte.model';

/** Resultado de "Cero Cuotas" (`CeroCuotasService.obtenerCeroCuotas`) — único bloque del legado (`rda/sectorista/cero_cuota/cero_cuota_sec_01`), título real "Cero y Una Cuota". */
export interface ReporteCeroCuotas {
  tabla1: TablaReporteResultado;
}
