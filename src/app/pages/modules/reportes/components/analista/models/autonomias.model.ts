import type { TablaReporteResultado } from '../../../models/tabla-reporte.model';

/** Resultado de "Autonomías" (`AutonomiasService.obtenerAutonomias`) — único bloque del legado (`LST_AUT_01`, `c_aut:3, aut:2` fijos). */
export interface ReporteAutonomias {
  tabla1: TablaReporteResultado;
}
