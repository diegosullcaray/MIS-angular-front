import type { TablaReporteResultado } from '../../../models/tabla-reporte.model';

/** Resultado de "Grupos por Vencer" (`GruposPorVencerService.obtenerGruposPorVencer`) — único bloque del legado (`rda/sectorista/grupo_pdm/grupo_pdm_sec_01`). */
export interface ReporteGruposPorVencer {
  tabla1: TablaReporteResultado;
}
