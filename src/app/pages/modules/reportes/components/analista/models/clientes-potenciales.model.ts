import type { TablaReporteResultado } from '../../../models/tabla-reporte.model';

/** Resultado de "Clientes Potenciales" (`ClientesPotencialesService.obtenerClientesPotenciales`) — único bloque del legado (`rda/sectorista/cli_pot/cli_pot_sec_01`). */
export interface ReporteClientesPotenciales {
  tabla1: TablaReporteResultado;
}
