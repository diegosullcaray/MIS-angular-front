import type { TablaReporteResultado } from '../../../models/tabla-reporte.model';

/** Resultado de "Clientes Nuevos y Recurrentes" (`ClientesNuevosRecurrentesService.obtenerClientesNuevosRecurrentes`) — único bloque del legado (`rda/sectorista/clientes_nuevos_recurrente/cliente_nuevo_rec_01`). */
export interface ReporteClientesNuevosRecurrentes {
  tabla1: TablaReporteResultado;
}
