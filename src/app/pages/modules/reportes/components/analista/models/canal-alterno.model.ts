import type { TablaReporteResultado } from '../../../models/tabla-reporte.model';

/** Resultado de "Canal Alterno" (`CanalAlternoService.obtenerCanalAlterno`) — único bloque del legado (`rda/sectorista/canal_alt/canal_alt_sec_01`). */
export interface ReporteCanalAlterno {
  tabla1: TablaReporteResultado;
}
