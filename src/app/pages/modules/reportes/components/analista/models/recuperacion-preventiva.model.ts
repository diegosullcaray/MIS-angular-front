import type { TablaReporteResultado } from '../../../models/tabla-reporte.model';

/** Resultado de "Recuperación Preventiva" (`RecuperacionPreventivaService.obtenerRecuperacionPreventiva`) — único bloque del legado (`rda/sectorista/recuperacion_preventiva/recuperacion_preventiva_01`). */
export interface ReporteRecuperacionPreventiva {
  tabla1: TablaReporteResultado;
}
