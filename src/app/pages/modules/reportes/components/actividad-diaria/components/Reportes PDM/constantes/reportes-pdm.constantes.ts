/** Códigos de reporte (`cod_rep`) de Reportes PDM. */
export const COD_REPORTES_PDM = {
  /** `seg-pdm`. */
  seguimientoPdm: 'SEG_PDM_01',
  /** `banca-solidaria` — motor `table.regular`, con el corte como `fec` con guiones. */
  bancaSolidaria: 'GRBSOLI_01',
} as const;
