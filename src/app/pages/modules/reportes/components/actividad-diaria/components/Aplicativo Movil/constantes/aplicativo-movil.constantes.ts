/** Códigos de reporte (`cod_rep`) de Aplicativo Móvil. */
export const COD_APLICATIVO_MOVIL = {
  /** `app_uso` — su host no agrega `fec`, va por `regularExacto()`. */
  usoApp: 'APP_USO_01',
} as const;
